import React from 'react';

interface TransactionIconProps {}

const TransactionIcon: React.FC<TransactionIconProps> = ({}) => {
  return <div>TransactionIcon</div>;
};

export default TransactionIcon;
